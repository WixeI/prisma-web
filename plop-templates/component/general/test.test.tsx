import { render } from "@testing-library/react";
import {{componentName}} from ".";

describe("something", () => {
  beforeEach(() => {
    render(<{{componentName}} />);
  });

  it("does something", () => {
    
  });

});
