import { render } from "@testing-library/react";
import Card from ".";

describe("something", () => {
  beforeEach(() => {
    render(<Card />);
  });

  it("does something", () => {
    
  });

});
