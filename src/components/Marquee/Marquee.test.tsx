import { render } from "@testing-library/react";
import Marquee from ".";

describe("something", () => {
  beforeEach(() => {
    render(<Marquee />);
  });

  it("does something", () => {
    
  });

});
