import { render } from "@testing-library/react";
import Contact from ".";

describe("something", () => {
  beforeEach(() => {
    render(<Contact />);
  });

  it("does something", () => {
    
  });

});
