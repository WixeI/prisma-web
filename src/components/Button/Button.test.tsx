import { render } from "@testing-library/react";
import Button from ".";

describe("something", () => {
  beforeEach(() => {
    render(<Button />);
  });

  it("does something", () => {
    
  });

});
