import { render } from "@testing-library/react";
import Step from ".";

describe("something", () => {
  beforeEach(() => {
    render(<Step />);
  });

  it("does something", () => {
    
  });

});
