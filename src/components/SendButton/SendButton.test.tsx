import { render } from "@testing-library/react";
import SendButton from ".";

describe("something", () => {
  beforeEach(() => {
    render(<SendButton />);
  });

  it("does something", () => {
    
  });

});
